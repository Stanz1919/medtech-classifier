# For Streamlit Community Cloud (and any plain `pip install -r requirements.txt`
# deploy): the third-party runtime dependencies this project has. The
# rules_engine / extraction / standards_mapper / cli packages use nothing
# outside the Python standard library - everything below is for the UI
# and its document/image upload feature. See pyproject.toml for the
# [dev]/[ui] optional-dependency groups this file mirrors the "ui" one
# of, and packages.txt (repo root) for the one non-Python dependency
# (the Tesseract OCR binary) Streamlit Community Cloud needs alongside
# this file.
#
# Versions below are floors, not exact pins - each is the version this
# feature was actually built and tested against, not just assumed
# compatible.
streamlit>=1.61
pypdf>=6.0
python-docx>=1.2
pytesseract>=0.3.13
Pillow>=11.0
