# For Streamlit Community Cloud (and any plain `pip install -r requirements.txt`
# deploy): the only third-party runtime dependency this project has. The
# rules_engine / extraction / standards_mapper / cli packages use nothing
# outside the Python standard library - see pyproject.toml for the full
# picture, including the [dev]/[ui] optional-dependency groups this file
# mirrors the "ui" one of.
#
# Pinned to >=1.61, the version this UI was actually built and tested
# against (st.badge and the width="stretch" sizing API are used directly -
# not verified against any older release).
streamlit>=1.61
